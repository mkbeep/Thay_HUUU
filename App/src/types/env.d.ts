declare module '@env' {
  export const EXPO_PUBLIC_API_URL: string;
  /** Cùng CUSTOMER_WEB_BASE_URL trên backend — URL công khai của Expo Web (\`npm run web\` / build \`web-app\`). */
  export const EXPO_PUBLIC_CUSTOMER_WEB_BASE_URL: string;
}
