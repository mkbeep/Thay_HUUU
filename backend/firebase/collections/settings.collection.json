{
  "collectionName": "settings",
  "description": "Cấu hình — document id `system` (seed-ancillary.ts). Top-level snake_case: updated_at, updated_by; map lồng nhau giữ key như code seed (camelCase trong restaurant, payment).",
  "documentId": "system",
  "fields": {
    "restaurant": {
      "type": "map",
      "description": "name, address, phone, email, taxRate, serviceFeeRate, …",
      "required": false
    },
    "payment": {
      "type": "map",
      "description": "enableCash, enableCard, enableMomo, enableZaloPay, enableBanking, …",
      "required": false
    },
    "updated_at": { "type": "timestamp", "required": true },
    "updated_by": { "type": "string", "description": "users.id hoặc 'seed-ancillary'", "required": false }
  },
  "notes": [
    "Có thể mở rộng loyalty, kitchen, … khi seed thêm field — cập nhật đồng bộ với scripts/seed-ancillary.ts."
  ]
}
